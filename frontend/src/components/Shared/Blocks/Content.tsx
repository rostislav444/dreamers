import styled from '@emotion/styled'

const Content = styled.div`
    position: relative;
    width: calc(100% - px);
    display: block;
    margin: 0 auto;
    left: auto;
    right: auto;
`
export default Content